- 每个 JS 文件顶部保留统一的 JSDoc 注释块（name/version/description/author/homepage/license），用于包元信息标注。
- 自定义单元格统一通过工厂函数（如 createNumberCell、createCheckboxCell、createFilterCell）返回 defineComponent，并在内部用 markRaw 包裹以避免 Vue 代理开销。
- 通过 getCurrentInstance() 向上遍历 parent 链查找最近的 StkTable 实例，以在任意 custom-cell 中访问表格上下文。
- 按需加载的大体积组件（Dropdown）采用动态 import('./xxx.js') 延迟加载，并缓存单例实例避免重复创建。
- 源码片段通过 `//#region src/...` 注释标注原始路径，便于 SourceMap 调试定位。